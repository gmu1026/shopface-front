import React from 'react';
import TimetableListContainer from '../containers/timetable/TimetableListContainer';

const IndexPage = () => {
  return <TimetableListContainer></TimetableListContainer>;
};

export default IndexPage;
